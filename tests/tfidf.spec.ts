"use strict"

import {expect} from "chai";
import 'mocha';

import Tfidf from '../lib/tfidf';

describe("TF IDF", function(){ 
    // it("Should get weightning", function(){ 
    //     expect(Tfidf.weight()).to.be.an("array")
    //     .to.have.members([1, 1.301, 1, 0])
    //     .to.have.length(2)
    // })
});